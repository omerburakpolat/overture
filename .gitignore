# macOS
.DS_Store

# Xcode
build/
DerivedData/
xcuserdata/
*.xcuserstate
*.xcscmblueprint

# SwiftPM
.build/
.swiftpm/
Package.resolved.lock

# Overture local state (never commit runtime artifacts)
.overture/

# Secrets — never commit signing, notarization or update keys
*.p12
*.p8
AuthKey_*
*.cer
*.certSigningRequest
*.provisionprofile
sparkle_private_key*

# Per-agent SPM scratch dirs
.build*/

# Release artifacts
build/

# Release staging (scripts/release.sh + generate_appcast output)
dist/
